# ── Client (shipped in the browser bundle — must be VITE_ prefixed) ──
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_MAPTILER_KEY=

# ── Server / API (/api functions only — NEVER VITE_ prefixed, never sent to the client) ──
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=        # the sb_secret_… key (bypasses RLS)
RSS2JSON_KEY=
FIRMS_MAP_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_PRO_MONTHLY=
STRIPE_PRICE_PRO_ANNUAL=

# ── Deferred (Portfolio add-on, post-revenue — not needed until rebuilt): SNAPTRADE_* ──
