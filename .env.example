# Copy this to .env.local and fill in values as needed.
#
# MAP TILES: MapLibre GL + Carto Dark Matter tiles are used by default (free, no key).
# To switch to Mapbox, set this token and update MapWidget.jsx.
VITE_MAPBOX_TOKEN=your_token_here
